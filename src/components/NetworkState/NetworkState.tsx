import React from "react";
import NetworkStateItem from "./NetworkStateItem";

/*function NetworkState({ onNetworkState }) {
    window.onoffline = () => {
        onNetworkState();
    };
    window.ononline = () => {
        onNetworkState();
    };
}export default NetworkState;*/